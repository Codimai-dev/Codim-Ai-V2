
export interface ToolBenefit {
  text: string;
}

export interface SalesTool {
  id: string;
  name: string;
  title: string;
  hook: string;
  benefits: string[];
  iconName: string;
  ctaText: string;
  category: string;
}
